// Covnert a string to an array

function stringToArray(string){

    return string.split(' ')
    
}

// Took the variable and add a split method
// Add a space within the split function then return it