// Testing 1-2-3

let number=function(array){
    return array.map((num, index) => `${index + 1}: ${num}`)
}

// Use .map with Array to return a new array for a, b, and c. i.e ["a","b","c"]
// Then have num display next to new array once it runs.

